{
    "modelfile": "wikiart.pth",
    "trainingdir": "../../wikiart/train",
    "validationdir": "../../wikiart/valid",
    "testingdir": "../../wikiart/test",
    "device": "cuda:1",
    "epochs": 10,
    "batch_size": 32
}
